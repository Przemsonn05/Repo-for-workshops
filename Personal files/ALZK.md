# ALZK – plik warsztatowy

W tym pliku możesz zmienić tekst oraz jedną wartość w kodzie.
Dzięki temu zmiana będzie dobrze widoczna w zakładce Changes w GitHub Desktop.

```python
def calculate_average(values: list[float]) -> float:
    return sum(values) / len(values)

print(calculate_average([10, 15, 20])
```
