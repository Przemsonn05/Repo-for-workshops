# WERA – plik warsztatowy

Przykładowe zadanie: dopisz nowy element do listy i zapisz zmianę jako commit.
Możesz też zmienić wiadomość wyświetlaną przez program.

```python
tasks = ["fetch", "edit", "commit", "push"]

for task in tasks:
    print(f"Krok: {task}")
```
